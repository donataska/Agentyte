OpenClaw Kanban

Private repo to store a simple Kanban-style board (Not started / In progress / Done).

Files:
- index.html — interactive static UI (drag/drop). Loads tasks.json.
- tasks.json — list of tasks (array of objects). Commit updates to this file to record changes.

How this will work:
- I will update tasks.json and commit/push changes when I mark tasks as Done.
- If you want a live site, enable GitHub Pages for the repo; index.html is the site entry.

Next steps:
1. Run `gh auth login` on this machine so I can create the private repo and push.
2. Tell me whether to create the repo under your user or an organization.
3. After auth I will create the repo named `OpenClaw-Kanban`, push the files, and enable Pages.

If you prefer Notion or GitHub Issues instead, tell me and I can switch.